// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  baseUrl: 'https://gist.githubusercontent.com/Tiagoperes',
  peopleUrl: '/1fa73c17081f40b6af23dbc53ca09cd3/raw/4a698cb4750ee14cab00e16146df4882d0d5000f/people.json',
  relationshipsUrl: '/1fa73c17081f40b6af23dbc53ca09cd3/raw/a0511cc534749ed2641a8b1f63a2944632540ca5/relationships.json',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
